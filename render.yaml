services:
  - type: web
    name: streamest-relay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run relay
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
